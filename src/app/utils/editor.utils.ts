import { ModalController } from '@ionic/angular';
import { CHARACTER_ID, SECTION } from '../constants/constants';
// import { EditionModalComponent } from '../components/modal/edition-modal.component';
import { Character } from '../model/character';

// export const edit = async (card: string, character: Character, modalController: ModalController) => {
//   const modal = await modalController.create({
//     component: EditionModalComponent,
//     componentProps: {
//       [SECTION]: card,
//       [CHARACTER_ID]: character.getId(),
//     },
//     backdropDismiss: false,
//     cssClass: 'modalParent'
//   });
//   await modal.present();
// };
